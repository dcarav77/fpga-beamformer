# Engineering Journal

## 2026-05-06: Project Setup

**What I did:**
- Created repo structure with clear directory names
- Defined workflow: learning → ready → hardware_ok → top

**What I learned:**
- `rtl/` = hardware design (Verilog)
- `sim/` = testing (testbenches)
- `build/` = Vivado output (never commit)

**Next steps:**
- Build blinky module in `rtl/learning/`
- Create testbench in `sim/testbenches/`
- Simulate and load to FPGA
